{
  "name": "oxion-monorepo",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "just dev",
    "build": "just build",
    "test": "just test",
    "lint": "just lint",
    "typecheck": "just typecheck",
    "generate": "just generate",
    "generate:contracts": "node ./scripts/generate-contracts.mjs",
    "generate:zod": "node --experimental-strip-types ./scripts/generate-zod.ts",
    "harness:freeradius": "node ./scripts/run-oxradius-freeradius-harness.mjs",
    "check:generated": "just check-generated",
    "build:gleam": "just build",
    "test:gleam": "just test",
    "lint:gleam": "just lint",
    "typecheck:gleam": "just typecheck",
    "gleam:build": "just build",
    "gleam:test": "just test",
    "gleam:format:check": "just lint",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "release:check": "changeset status --verbose",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
