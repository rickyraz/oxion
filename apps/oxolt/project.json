{
  "name": "oxolt",
  "root": "apps/oxolt",
  "projectType": "application",
  "tags": [
    "scope:apps",
    "domain:oxolt",
    "lang:gleam",
    "type:app"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxolt",
        "command": "gleam build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxolt",
        "command": "gleam test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxolt",
        "command": "gleam check"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxolt",
        "command": "gleam format --check src test"
      }
    }
  }
}
