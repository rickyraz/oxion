{
  "name": "oxbill",
  "root": "apps/oxbill",
  "projectType": "application",
  "tags": [
    "scope:apps",
    "domain:oxbill",
    "lang:gleam",
    "type:app"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxbill",
        "command": "gleam build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxbill",
        "command": "gleam test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxbill",
        "command": "gleam check"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/oxbill",
        "command": "gleam format --check src test"
      }
    }
  }
}
