# frontend/platform

TanStack Start app placeholder untuk Platform Mode.

Status saat ini: scaffold only.

Next:

1. Inisialisasi TanStack Start project.
2. Hubungkan kontrak generated types dari `/generated`.
3. Aktifkan `build`, `typecheck`, `lint`, dan `test` nyata di `package.json`.
