/**
 * Legacy placeholder for schema-first extensions.
 *
 * Current active generators:
 * - scripts/generate-contracts.mjs (Gleam package-interface -> TS contracts)
 * - scripts/generate-zod.ts (interface.json -> Zod schemas)
 */
export {};
