# packages

Folder shared libraries lintas app (TS/Gleam).

Status saat ini:

- `policy/` aktif sebagai package policy engine bersama.
- `interop/` aktif untuk modul kontrak/interop lintas app (contoh: `oxion/radius/coa/result`).
